using Sirenix.OdinInspector;

namespace GameplayAbilitySystem
{
    public enum EAttributeModifier
    {
        [LabelText("加")]
        Add,
        [LabelText("乘")]
        Multiply,
        [LabelText("覆写")]
        Override
    }
}